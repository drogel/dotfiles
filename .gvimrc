" ======= Vim GUI settings =======
"
"

" Sets snytax highlight.
syntax enable

" Sets dark background.
set background=dark

" Use 12pt Monaco.
set guifont=Monaco:h12

" Better line-height.
set linespace=8

" Show (partial) command in the last line of the screen.
set showcmd

" If in Insert, Replace or Visual mode put a message on the last line.
set showmode

" Show the line number relative to the line with the cursor in front of each
" line.
set relativenumber

" When relativenumber is on, shows the absolute line number only for the line
" in which the cursor is currently on.
set number

" When on, lines longer than the width of the window will wrap and displaying
" continues on the next line.
set nowrap

" Searches wrap around the end of the file.  Also applies to |]s| and |[s|,
" searching for spelling mistakes.
set wrapscan

